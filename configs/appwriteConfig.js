// import { Client, Storage } from "node-appwrite";

// // Initialize Appwrite client and storage
// export const initializeAppwrite = () => {
//   const client = new Client();
//   client
//     .setEndpoint(process.env.NEXT_PUBLIC_APPWRITE_ENDPOINT) // Replace with your Appwrite endpoint
//     .setProject(process.env.NEXT_PUBLIC_PROJECT_TD) // Replace with your Project ID
//     .setKey(process.env.NEXT_APPWRITE_APKIKEY); // Replace with your API Key

//   const storage = new Storage(client);
//   return storage;
// };
